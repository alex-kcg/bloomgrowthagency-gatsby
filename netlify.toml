[build]
  publish = "public"
  command = "sed -i "s|BRANCH_PLACEHOLDER|${BRANCH}|g" config.yml && npm run build"
[build.environment]
  NODE_VERSION = "14.15.0"
  YARN_VERSION = "1.22.4"
  YARN_FLAGS = "--no-ignore-optional"
